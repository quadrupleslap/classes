import React from 'react';
import Centered from './centered';

export default React.createClass({
  render() {
    return <Centered vertical horizontal>Coming soon (like in the next few days)!</Centered>;
  }
});